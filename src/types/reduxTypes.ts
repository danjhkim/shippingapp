import { User } from './OrderTypes';

export interface OrdersState {
	ordersData: Object | null;
	status: String | null;
	error: String | null;
}

export interface OrdersState {
	summaryData: User | null;
	status: String | null;
	error: String | null;
}
